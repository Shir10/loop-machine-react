import initialState from "../../store/initialState";

const PadReducer = (state = initialState.pad, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default PadReducer;